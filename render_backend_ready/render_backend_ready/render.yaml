services:
  - type: web
    name: family-auto-share-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend_fastapi:app --host 0.0.0.0 --port $PORT
    plan: free
